# Memory

## Durable
- Lean: workspace has no default Lake target; plain 'lake build' produces no SpherePacking.olean. Build '+SpherePacking' explicitly (lean-action build-args, bump-mathlib) — verified lakefile.toml + .github/workflows/lean.yml.
